{
  "title": "Intro to Py",
  "day": "25",
  "month": "10",
  "year": "2020",
  "desc": "For homework we were given 3 very simple tasks in Python.",
  "category": "Homework",
  "language": "Python",
  "showLine": "40",
  "input": "",
  "output": "",
  "code": [
    "#Task 1",
    "#Input is original price and discount in % and string (output option)",
    "#String defines if program prints out discount value or final price",
    "original = float(input(\"Original price: \"))",
    "discount = float(input(\"Discount in percent: \"))",
    "option = input(\"Option(price/discount): \")",
    "discountValue = original * discount/100",
    "",
    "if option == \"discount\":",
    "    print(\"Discount value is: \", discountValue)",
    "else:",
    "    print(\"Final price: \", original - discountValue)",
    "",
    "",
    "#Task 2",
    "#Input is the length of a side of a equilateral triangle",
    "#Second input is a number between 1 and 4 that determines the output",
    "#Output can be area, perimeter, radius of the circumscribed circle or height",
    "#Note: result should be rounded on 5 decimals",
    "import math",
    "",
    "a = float(input(\"Lenght of a triangle's edge: \"))",
    "n = int(input(\"Number(1-4): \"))",
    "",
    "if n == 1:",
    "    print(\"Area is: \", round(((math.sqrt(3)/4) * a * a), 5))",
    "elif n == 2:",
    "    print(\"Perimeter is: \", round(3 * a, 5))",
    "elif n == 3:",
    "    print(\"Radius of the circumscribed circle is: \", round(((math.sqrt(3)/3) * a), 5))",
    "else:",
    "    print(\"Height of triangle is: \", round(((math.sqrt(3)/2) * a), 5))",
    "",
    "",
    "#Task 3",
    "#Using while loop take inputs from user until the input is divisible by 7",
    "#Calculate the arithmetic number of all even numbers",
    "#Calculate the product of all numbers divisible by 5",
    "#Output should be printed inline with space inbetween",
    "summ = 0",
    "divisor = 0",
    "product = 1",
    "number = 1",
    "",
    "while number % 7 != 0:",
    "    number = int(input())",
    "    if number % 2 == 0:",
    "        summ = summ + number",
    "        divisor = divisor + 1",
    "    if number % 5 == 0:",
    "        product = product * number",
    "",
    "print(summ/divisor, \" \", product)",
    ""
  ]
}