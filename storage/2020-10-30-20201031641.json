{
  "title": "Lists & iteration",
  "day": "30",
  "month": "10",
  "year": "2020",
  "desc": "Small homework after class. Store inputs in list and manipulate them for the output.",
  "category": "Homework",
  "language": "Python",
  "showLine": "22",
  "input": "",
  "output": "",
  "code": [
    "#NOTE: Do not add any extra text on input/output",
    "",
    "#Task 1",
    "#Ask for integer input n, then take n amount of strings from user and store in list",
    "#After storing words, ask user to input a letter",
    "#Using list comprehension, create a new list which contains only elements from initial",
    "#list of words that start with the given letter",
    "n = int(input())",
    "words = []",
    "",
    "for i in range(n):",
    "    w = input()",
    "    words.append(w)",
    "",
    "letter = input()",
    "",
    "output = [w for w in words if w[0] == letter]",
    "",
    "print(output)",
    "",
    "",
    "#Task 2",
    "#Ask user to input positive numbers untill he inputs 0",
    "#All numbers except 0 should be stored in a list.",
    "#Write out all elements of a list that are greater than half times the average of list",
    "l = []",
    "sum = 0",
    "",
    "while True:",
    "    n = float(input())",
    "    if n == 0:",
    "        break;",
    "    sum = sum + n",
    "    l.append(n)",
    "",
    "print([x for x in l if x > (1/2)*(sum/len(l))])",
    "",
    "",
    "#Task 3",
    "#Ask user to input size of list n and then n strings that are going to be stored in a list",
    "#Ask user to input a letter",
    "#From a given list, you need to create a new list with elements being a 2 element list.",
    "#First element of a sublist is going to be whole number representing the number of letters",
    "#in a word that are the same as the previously input letter.",
    "velicina = int(input())",
    "words = []",
    "list = []",
    "",
    "for i in range(velicina):",
    "    n = input()",
    "    words.append(n)",
    "",
    "slovo = input()",
    "",
    "for word in words:",
    "    n = 0",
    "    for letter in word:",
    "        if letter == slovo:",
    "            n = n + 1",
    "    list.append([n, word])",
    "",
    "print(list)",
    ""
  ]
}