{
  "title": "Manipulatin files",
  "day": "12",
  "month": "11",
  "year": "2020",
  "desc": "We written in and read from files with Python.",
  "category": "Class",
  "language": "Python",
  "showLine": "57",
  "input": "",
  "output": "",
  "code": [
    "#Write a function that takes a letter and as result returns true if the letter is aeiou",
    "#otherwise false. For a string, using list comprehension and above defined function,",
    "#make a new string so that you drop out aeiou from the original.",
    "def is_aeiou(letter):",
    "    aeriou = ['a','e','i','o','u']",
    "    return letter.lower() in aeiou",
    "",
    "sentence = \"Vani je danas oblacno.\"",
    "l = [x for x in sentence if not is_aeiou(x)]",
    "sentence = ''.join(l)",
    "print(sentence)",
    "",
    "",
    "#Create a funcion that takes string that contains average grade of student in shape",
    "#\"subject1=grade1;subject2=grade2\" and string with name of a student.",
    "#Function should create a list and then dictionary from a list using str.split()",
    "#and calculate the average grade. Then prints out grade using a) %s %f b) format func c) f-string",
    "def avg(subjects, name):",
    "    l = subjects.split(';')",
    "    dict = {}",
    "    for p in l:",
    "        dict[p.split('=')[0]] = float(p.split('=')[1])",
    "    suma = 0",
    "    for k in dict:",
    "        suma += dict[k]",
    "    average = suma/len(dict)",
    "",
    "    print('Student %s has a grade %.2f.'%(name, average))",
    "    print('Student {} has a grade {:.{}f}.'.format(name,average,2))",
    "    print(f'Student {name} has a grade {average:.2f}.')",
    "",
    "avg('math=3.5;history=4.2;physics=4.6','Ivo')",
    "",
    "",
    "#Write a function that takes a list of strings as argument and returns the longest and it's length",
    "#With each call you should open calls.txt and on the end of file add arguments and result of function",
    "def longest_str(lista):",
    "    article_file = open('calls.txt','a')",
    "    file.write('arguments: [')",
    "    for l in lista:",
    "        file.write(l+' ')",
    "    file.write('] \\n')",
    "    longest = lista[0]",
    "    for el in lista:",
    "        if len(el) > len(longest):",
    "            longest = el",
    "    file.write('result: (' + longest + ', ' + str(len(longest)) + ') \\n\\n')",
    "    file.close()",
    "    return (longest, len(longest))",
    "",
    "print(longest_str(['lista','koja','sadrzi','stringove']))",
    "",
    "",
    "#In file asortiman.txt is asortiman of a shop and prices and categories of articles",
    "#Write a function that's for a choosen article going to take whole rang and list of categories amd create",
    "#a new text file to store items whose prices are in a price rang and are under one of categories.",
    "def chosen(price_min, price_max, categories):",
    "    article_file = open('asortiman.txt')",
    "    file = open('chosen_article.txt','w')",
    "    new_article = []",
    "    first = article_file.readline()",
    "    file.write(prvi)",
    "",
    "    for l in article_file:",
    "        lineSplit = l.strip().split(';')",
    "",
    "        if price_min <= float(lineSplit[1]) <= price_max and lineSplit[2] in categories:",
    "            new_article.append(lineSplit)",
    "            file.write(l)",
    "    file.close()",
    "    article_file.close()",
    "    return new_article",
    "",
    "n_article = chosen(5,200,['Prehrana','Kozmetika','Uredski pribor'])",
    "print(n_article)"
  ]
}